#Some tools to work conveniently with docker containers

**Usage**
    dofind match
Returns the ID of the container matched by 'match'    
    doexec match command
Run the command in the container matched by 'match'

